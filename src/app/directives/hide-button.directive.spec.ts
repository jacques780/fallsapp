import { HideButtonDirective } from './hide-button.directive';

describe('HideButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new HideButtonDirective();
    expect(directive).toBeTruthy();
  });
});
